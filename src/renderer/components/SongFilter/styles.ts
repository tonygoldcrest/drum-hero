import styled from 'styled-components';
import themedark from '../../theme';

export const Wrapper = styled.div`
  flex-grow: 1;
`;

export const SongCount = styled.div`
  color: ${themedark.color.textFaint};
  font-size: ${themedark.fontSize.small}px;
`;
